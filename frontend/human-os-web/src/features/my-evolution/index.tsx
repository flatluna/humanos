import { ComingSoonPage } from '@/components/layout/ComingSoonPage';
import { TrendingUp } from 'lucide-react';

/** "My Evolution" — growth across the six Human OS layers over time.
 *  TODO: This will visualize the Evolution Journey historically (not
 *  just the current layer, as shown compactly on Today).
 */
export function MyEvolutionPage() {
  return <ComingSoonPage titleKey="myEvolution" icon={TrendingUp} />;
}
