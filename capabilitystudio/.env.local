# Local-only override: HumanOS backend is running on 7072 instead of the
# default 7071 because port 7071 was occupied by an unrelated project
# (HolisticFrameworkProject) on this machine (2026-07-23).
VITE_API_BASE_URL=http://localhost:7072/api
